# ===========================================
# Portfolio — Dokploy Environment
# Paste into Dokploy → Environment
# Domain/TLS dikonfigurasi di tab Domains (bukan di sini)
# ===========================================

NODE_ENV=production

# Port internal container (harus sama dengan Container Port di Dokploy Domains)
# nginx di image listen di 3000
APP_PORT=3000

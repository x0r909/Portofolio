# Portfolio — Dokploy Environment
# Paste ke Dokploy → Compose → Environment

NODE_ENV=production

# Port di dalam container (nginx listen)
APP_PORT=3000

# Port di host LXC Dokploy (hindari 3000 — dipakai Dokploy UI)
# NPM forward ke http://<IP-LXC-Dokploy>:HOST_PORT
HOST_PORT=3080

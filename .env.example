# ===========================================
# Portfolio — Dokploy + Cloudflare Tunnel
# Copy to .env / paste into Dokploy Environment
# ===========================================

# Host port exposed for cloudflared (tunnel → http://127.0.0.1:HOST_PORT)
HOST_PORT=3000

# Runtime
NODE_ENV=production
